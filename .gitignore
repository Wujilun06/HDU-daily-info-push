# Python
__pycache__/
*.py[cod]
*.pyc
*.pyo
venv/
.venv/
env/
.mypy_cache/
.pytype/

# C++
*.o
*.obj
*.exe
build/
cmake-build-*/

# Node / Web
node_modules/
dist/
.cache/

# Android
.gradle/
local.properties
*.apk
*.aar
app/build/

# 环境变量与密钥
.env
*.key

# 编辑器与系统
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# 日志
*.log

# 隐私：本地工作日志
.workbuddy/
daily_digest/.workbuddy/

# ---------------------------------------------------------------------------
# 隐私文件（本项目唯一需要忽略的隐私内容；所有隐私都集中在这一份文件里）
# 任何步骤都从 private_config.json 读取隐私，不再有零散的 cookie / key 文件。
# 模板 private_config.example.json 可入库；真正的 private_config.json 不入库。
# ---------------------------------------------------------------------------
private_config.json

# 本地登录二维码 / 页面截图（由 login 生成，非隐私但无需入库）
weread_login_*.png

# 运行日志（保持本地，不入库）
daily_digest/*.txt
!daily_digest/requirements.txt
